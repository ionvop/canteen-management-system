import AppShell from "@/components/layout/AppShell";
import MenuBrowser from "@/components/menu/MenuBrowser";

const Menu = () => {
  return (
    <AppShell title="Menu">
      <MenuBrowser />
    </AppShell>
  );
};

export default Menu;